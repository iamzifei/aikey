# 更新日志

> 版本号**按技能各自记**（每个 `SKILL.md` 的 `version:` 字段），不是整仓一个号。
> 源码版本记录在 `SKILL.md`；ClawHub 当前脚本沿用其线上版本递增补丁号。整包 Release / SkillHub 使用独立发布号。
>
> 本文件从 2026-09-05 开始记。**在此之前的改动在 git 历史里**，没有回溯补写 —— 补写出来的条目是编的，不是记的。

---

## 2026-09-23 · 整包 v1.0.6：frontmatter 按目的地分发

- **实测依据**（澳洲AI教父 的豆包录屏）：豆包安装时会判定 frontmatter 里有「不被允许的字段」，然后**自己改写 SKILL.md** 再装。装得上，但多绕一圈，改成什么样也不在我们手里。
- 改法：`build_bundle.py` 加 `--skillhub` 开关。**发到 GitHub 的包（豆包与手动上传用）只留 `name` / `description` / `version`**；SkillHub 那份保留 `slug` 与 `displayName`（它靠这两个显示中文名）。
- 录屏里那一步是折叠的，**没写出具体是哪几个字段**，所以没有猜，而是从源头只发标准字段。
- GitHub About 补上个人主页 <https://澳洲AI教父ai.dev>；About 文案保持「25 个技能」——短命令别名不算一个技能。

## 2026-09-23 · 换上改名后录的豆包演示，并纠正豆包的用法说明

- 两段演示 GIF 换成改名后新录的：安装那段结尾正好收在「技能安装完成」，使用那段是选中技能卡片后直接说话。
- 🔴 **纠正一处说明**：此前 README 写「豆包输入 `/key`」，**和实际操作不符**。录屏里的真实用法是**在输入框选中技能**（卡片上显示的是 `aikey-toolbox`），然后直接说人话，不敲斜杠命令。表格已按平台分开写：Claude Code / Codex / Cursor / ClawHub 用 `/key` 或 `/aikey`；WorkBuddy 用 `/key`；豆包选卡片。
- 这条是看你发来的录屏逐帧核出来的，不是猜的。

## 2026-09-23 · 新增短命令别名 `key`（第 26 个目录，但不算第 26 个技能）

- 平铺安装（Claude Code / Codex / Cursor / ClawHub）现在也能敲 **`/key`**：新增一个只做转发的别名技能，读同级 `aikey/SKILL.md` 并按它执行，**不复制任何判据**（判据写两份，改的时候必然有一份过期）。
- 描述里写死一句「**与 API key、密钥、密码、令牌无关**」——它要待在一个装了四十多个技能的目录里，不这么写容易被「我的 API key 过期了」这类问题误触发。
- 工具链六处显式接好：别名不带家族前缀，通配符抓不到它（构建复制 · ClawHub 发布 · 安装脚本 · 入口的技能发现 · 一致性闸 · 打包版）。
- **一致性闸把别名排除在套件计数外**：它不是一项能力，算进去会让「生意操盘手（9 个）」变成 10 个。
- **打包版不收它**：打包版本身已经注册 `key`，再塞一个转发模块就是自己指自己。

## 2026-09-23 · 打包版 v1.0.4：命令改成 `/key`，介绍重写

- **打包版（WorkBuddy / SkillHub / 豆包）的命令从 `/aikey` 改成 `/key`**。那边整个包只注册一个命令，`aikey` 每次都要敲全太长。**平铺版不动**（Claude Code / Codex / ClawHub 仍是 `/aikey-*`）：那边 25 个技能各是一个命令，改一次等于整套改名再走一遍，而且 `key` 在共享技能目录里太通用。两者分开由 `skillprefix.BUNDLE_CMD` 控制。
- **介绍重写**：原来是一大段把 25 个技能名铺开、触发方式埋在最后。现在按「一句话是什么 → 做内容一行 → 看生意一行 → 怎么用」四段写，`/key` 单独成段。
- 🔴 **修掉一处会让用户敲空的说明**：原介绍和包内路由表写着 `/aikey-topic`、`/aikey-script` 这些命令，**但打包版里它们不存在**（包只注册一个命令，成员是包内文件）。表格改成「用户点名的能力 → 读哪个文件」，并写明别让用户去敲它们。

## 2026-09-23 · 发布工具：ClawHub 发布串行化

- 工作流加 `concurrency` —— 当天三次推送并发跑，小任务先完成、大任务后完成并用更旧的树覆盖了新内容，线上停在上一版半小时。排队之后最新的树天然最后发。
- 公开构建加清理步骤：源码里已删除的文件会从产出目录删掉。改名当天残留的旧技能目录和旧结构图各害过一次 —— 残留会让图片数量闸在**署名归一之前**退出，留下一棵带真名的半成品树。
- 记录纠正一条自己的错判：ClawHub「上传成功」后有一段 pending，**可长达十分钟以上**；期间 `inspect --version` 会返回 `Version not found`。当时据此断定「上传没落地」是错的，几分钟后它自己转正了。判据应为 `inspect` 的 `Latest` + 试装，并留足时间。

## 2026-09-23 · README 配图跟上改名（技能内容未改，整包未升版）

- 编排结构图改名 `zmm-map.svg` → `aikey-map.svg`，图内四处更新：标题、`/zmm` → `/aikey`、内容操盘手 15 → **16 个**、小老板看盘 7 个 → **生意操盘手 9 个**。
- 两段豆包演示 GIF 录于改名之前，**画面里仍是旧名字**。没有假装它们是新的：alt 文字改回与画面一致，并在图下写明「录于改名前，步骤与地址不变，旧地址会自动跳转」。重录要本人操作，进 HUMAN QUEUE。
- 两张豆包上传对话框截图是通用 UI，无品牌名，不用动。

## 2026-09-23 · 整包 v1.0.3：中文名「AI 钥匙」· 套件二改名「生意操盘手」

- **中文名定了：AI 钥匙**（英文仍是 AI KEY，命令仍是 `/aikey`，中文没法当命令）。README 开头说明两个名字等价，并把「25 个技能 = 25 把钥匙，一把开一把锁」写成正文。
- **套件二「小老板看盘」改名「生意操盘手」**。原名两个毛病：「看盘」是股票黑话，第一眼看不懂；「小老板」把用户说小了。新名和既有的「内容操盘手」成对——一个操内容，一个操生意。
  （排除过的：「生意参谋」撞阿里的卖家数据产品；「生意诊断」盖不住决策、押注、追踪那几个技能。）
- **顺带修掉一批过期数字**：打包版描述写着「做内容 15 个 / 看生意 7 个」、两张表写着 13 个和 7 个，实际是 16 和 9；技能清单漏了短剧编剧、从哪儿下手、风口在哪三个，已补。
- GitHub About 同步。

## 2026-09-23 · 整包 v1.0.2：改名预告段换成一句过去时说明

- 25 个技能开头那段「这套技能要改名了……下一版起」是给改名**之前**的用户看的，改名完成后它在新名字的技能里已经过期。换成一句：本技能原名 `/zmm-…`，v1.0.0 起改名为 AI KEY，旧名字不再更新，升级见 README。
- 发现方式：核验 v1.0.1 的下载包时读包内文件读到的，不是看源码看出来的。

## 2026-09-23 · 整包 v1.0.1：去掉描述前的图标符号，README 补升级指引

- 技能描述前注入的 `📐` 前缀去掉（公开构建与打包版各两处），页脚同步。`metadata.openclaw.emoji` 这个平台图标字段保留。
- README：改名说明由「即将改名」改成已完成口径，并补齐四个渠道各自怎么升级、以及旧链接为什么不会断。
- GitHub About 更新：原文还写着旧品牌和「20 个技能」，现为「AI KEY · 内容操盘手 + 小老板看盘 —— 25 个中文 AI 技能」。

## 2026-09-23 · 整包 v1.0.0：改名完成，这套技能现在叫 AI KEY

- 命令 `/zmm-…` → `/aikey-…`；25 个技能目录与 frontmatter 改名；中文显示名前缀改为「AI KEY·」；对外署名改为账号现名「澳洲AI教父」。
- 公开仓库 `iamzifei/zmm` → `iamzifei/aikey`；SkillHub 打包版换新 slug `aikey-toolbox`。
- **本版只改名字**，判据与流程一个字未动 —— 这样渠道若出问题，归因是唯一的。积压内容已在 v0.15.0 清空。
- **老链接全部保住**（四种组合实测 200 且 SHA 一致）：仓库改名自动跳转；同一份包同时挂 `aikey-toolbox.zip` 与 `zmm-toolbox.zip`；SkillHub 旧 slug 也发了同内容版本并标明改名。
- 改名不会让技能读不到记忆：公约〈六〉新增回退 —— 读不到 `aikey-*/` 就读旧的 `zmm-*/` 并说明。
- 🔴 **发版核验记一条**：`latest/download/…` 这条路径有 CDN 缓存，头两次返回的是上一版的包；按 tag 直取或带随机参数重试才准。

## 2026-09-23 · 整包 v0.15.0：改名预告版（内容未改）

- **这套技能将改名为 AI KEY**：命令从 `/zmm-…` 变成 `/aikey-…`，公开仓库改名为 `iamzifei/aikey`，SkillHub 打包版换新 slug `aikey-toolbox`。**本版只加预告，不改任何判据和流程。**
- 25 个技能的 `SKILL.md` 开头统一加了一段预告，写清新命令和升级的两条命令；每个成员补丁号各升一位。
- **为什么要有这一版**（2026-09-23 实测，临时仓库 + 隔离主目录，测完已删）：改名之后，用 skills.sh 装的老用户执行 `update` 只会看到「上游好像删掉了这些技能」然后跳过，**新名字装不上、旧的停在老版本，也就永远收不到任何通知**。所以通知必须赶在改名**之前**发出去。
- 同批实测的其余结论：GitHub 仓库改名后**旧下载链接会 301 跳到新仓库**（SHA 一致），旧 git 地址同样跳转；同一个包挂新旧两个文件名可行；`npx skills remove -g` 能干净删除旧版。
- README 增加「从 zmm 升级到 AI KEY」一节。
- 下一版 v1.0.0 是改名版，**diff 里只有名字**，内容改动一律不搭车。

## 2026-09-22 · 引号统一为弯引号 · 短剧台词豁免黑话禁令 · zip 中文文件名修复 · 整包 v0.14.0

- **引号（澳洲AI教父 2026-09-22 定）**：成稿（口播稿、标题、封面、帖子、剧本、简介）里的引号与强调**一律用弯引号 “ ”**，禁的是英文直引号 `""`；规则文件、技能正文、vault 内部文档可以继续用「」。此前的「禁双引号、强调用「」」只是一条文风规定，没有效果数据支撑（来源记忆只写了「是文风红线」）。改动：`config` 的 `voice.dont` 与 `emphasis_mark`、交互规范 §一，以及写死了双引号检查的 zmm-hook（红线闸门）、zmm-title、zmm-review、zmm-flow。
- **短剧台词**：`/zmm-drama` 虚构角色的台词在违禁词之外，**黑话禁令也一并豁免**（照扫、只提醒不改；剧名、简介、封面不在例外内）。家族公约 §三 第 4、6 行同步。
- **zip 中文文件名**：新增 `scripts/pack_bundle.py` 取代手工 `zip -q -r -D -X`。旧包的中文文件名没有 UTF-8 标志（v0.11.0–v0.13.0），严格按标志解码的工具（例如 Windows 自带解压）会显示乱码；新包 50 个中文文件名全部带标志，文件清单与 v0.13.0 逐项一致，同一目录打两次 SHA 相同。资产名固定为 `zmm-toolbox.zip`，不再靠手工约定。⚠️ macOS 自带 `unzip`（6.00，2009）不认这个标志，列目录仍显示乱码，`bsdtar` 与 `ditto` 正常。
- 🔴 **ClawHub 拦截修复**：zmm-cut 线上 0.2.7 被 ClawHub 判为恶意（`clawscan` verdict: malicious），扫描结果已下载核对。原因是规则卡里「画面给证据、口播不念数字，把风险分到两条审核通道」「口播念了画面上的数字就删口播、不删画面」—— 这是在规避平台审核。zmm-review 规则卡有同一条（「三档尺度管口播不管画面」），尚未被扫到，一并改。**新口径**：收入、营收、收款的具体金额在**画面、口播、字幕、标题同一个标准**，按 `config` 卖真红线哪里都不出现；截图里的金额同样涂掉；不按「哪条通道查得严」决定数字放在哪。这也消掉了它和 `config` `sell_real.forbidden`「公域出现具体收入金额」的长期矛盾。zmm-review 的一份历史评测里的同类建议标为作废。
- 🔴 **发布前自查（模仿 ClawHub 审核视角，独立 context）又找出 zmm-review 里一批同类问题，一并改**：违禁词「命中即替换、谐音化」改为「换意思真正不同的说法或删除，不用谐音、拆字、代号」；删除「平台安全替代说法表」（钱→米、人民币→块/米、美元→美刀、酒局→8+1），改为「不用代称指钱或违禁的东西」；「替代词不是黑话、放行」改为「暗号式替代词单独报，建议改说法或删」；保留力度版不再「劲儿留着」；「画面补边界」不再「口播不动」，改为先改口播、小字只作补充；倍数口径写明比值必须真实、不暗示观众照做能得到同样回报；导流命中建议删掉，不建议用 vx、谐音伪装联系方式；一份历史评测里的「多赚了多少米」等建议按新规则改写。
- zmm-drama 台词豁免补写理由：两张词表管的是创作者自己的措辞，**不是因为台词不被审核**，内容红线在台词里全套生效。
- 同一次扫描的三条「可疑」一并处理（zmm-cut）：触发词收窄（去掉「加字幕」「重新排一下顺序」这类普通说法）；写明是中文口播工作流；默认参数只在确认是詹明明账号时直接用，否则先问。
- 🔴 **发布脚本 bug 修复**（`scripts/publish_clawhub.sh`）：被审核拦下的版本会从 `inspect` 里隐藏，`latestVersion` 停在上一个版本，脚本于是又算出被拦的那个版本号，上传报 `already exists`，旧代码把它当成「内容未变」计为成功——这次修复版 zmm-cut 就这样没传上去，日志一片绿。现在遇到 `already exists` 自动加补丁号重试（最多 5 次），只有平台明确说没变化才算跳过，其余一律报失败。已用模拟的 clawhub 命令测过两条路径。本次 zmm-cut 以 0.2.8 手动补传。
- 版本：zmm 0.3.1 · zmm-hook 0.4.2 · zmm-title 0.2.4 · zmm-review 0.4.2 · zmm-flow 0.3.1 · zmm-drama 0.2.1 · zmm-cut 0.4.1。

## 2026-09-22 · zmm-drama 0.2.0 · zmm-cut 0.4.0：接入 Seedance 视频生成提示词 · 整包 v0.13.0

- `zmm-drama` 0.1.0 → **0.2.0**：新增可选 **Phase 7「转分镜与生成提示词」**。剧本定稿后切成分镜表（每条生成默认 ≤15 秒、最长 30 秒，每段 2–5 秒一个动作），再逐条写成 Seedance 能直接用的提示词：全局 → 固定（人物只写一次）→ 时间轴 → 台词 → 约束；人物用参考图锁长相，并写明「不继承」参考图的背景、姿势、构图、光线；画面里不生成任何文字。新参考 `references/分镜与生成提示词.md`。
- `zmm-cut` 0.3.0 → **0.4.0**：工作流新增 **④b「B-roll / 生成素材」**（仍是用户明确要才做）。逐句判哪几句能配生成画面：比喻、过程、氛围、转场可以；**「我做过」「真实发生」这类证言、任何数据截图、讲 AI 时「AI 在工作」的画面，一律不用生成画面**。提示词要贴手机实拍质感（点名器材、主动关掉电影感，不和电影级打光混写）。新参考 `references/生成素材.md`，红线表加一行「生成素材不作证」。
- 两个技能都**不自己出片**：按家族公约「归口调用」交给 `chatcut-video-gen`（需 ChatCut 付费额度），🔴 **提交前报条数、时长、模型，等用户确认花费**，一次只提交勾过的那几条。
- 合规：发布 AI 生成的画面时提醒打开平台的「AI 生成」声明（《人工智能生成合成内容标识办法》2025-09-01 施行，第十条，原文已核）。
- 🔴 **提示词写法来自公开案例，未实测出片**（本次 ChatCut 处于登出状态），两个技能都要求照实告诉用户「出片要试」。
- 外部方法来源不写进技能，指纹已加进私有泄漏表。中文名未改。
- 整包 v0.13.0：成员改动是 zmm-drama 0.2.0、zmm-cut 0.4.0，其余 23 个未改。

## 2026-09-22 · 新技能 zmm-drama 0.1.0：短剧编剧 · 整包 v0.12.0

- 新增第 25 个技能 `zmm-drama`（詹明明·短剧编剧），内容操盘手 15 → 16，整包 24 → 25。入口是**虚构的剧情、人物、多集故事**：给一个选题或一段初始内容，写出开头抓人、结尾留得住人的竖屏短剧剧本。与 `zmm-script`（真实经历 / 知识口播）、`zmm-hook`（只管开头）边界互斥。
- 两种产出，开头用一道选择题问：① **单集短剧**（1–3 分钟，自成一体，账号自己能拍）；② **付费连续剧开发包**（一句话卖点 + 梗概 + 人物小传 + 整季分集大纲并标付费卡点 + 前 3 集完整剧本，分三段交付、每段停一次）。
- 方法：冲突要长在设定里（「删掉任意一集，下一集还成立 ⇒ 那集是现编的」）；单集按「开场爆点 → 冲突 → 最大冲击 → 停在问题上」切；开头六式出 3 个候选、结尾两式（落地 / 留问）；**每个反转都要能指出伏笔在哪一秒**；台词每句推进冲突或露出人物。
- 🔴 卡点集数（80 集约 8–12 / 26–30 / 60）与单集秒数全部标为**经验值**，按总集数换算，不当规律说。
- 红线：国内付费剧提醒备案（按 2024 年广电分级规定，提示查最新要求）；原创（只借类型不借设定与名场面）；改编真人真事时卖真与隐私全套生效。
- 外部方法来源不写进技能，指纹已加进私有泄漏表，公开构建自动拒绝。
- 红线例外（澳洲AI教父 2026-09-22 定）：`/zmm-drama` 剧本里**虚构角色的台词豁免违禁词**——照扫、只提醒不改；剧名、简介、封面不在例外内。已写进家族公约 §三 第 4 行。
- 修复：`build_public.sh` 的中文名表**一直漏了 `zmm-path` 和 `zmm-trend`**，两个技能在 v0.10.0 / v0.11.0 的公开产物里缺 `slug:` / `displayName:` / emoji。已补上，并给 `check_consistency.py` 加了第 4 项检查：**每个产出 SKILL.md 必须带 displayName**（旧产物上实测报出这两个，补表后 25/25 通过）。两个技能的源码版本未改。
- 整包 v0.12.0：成员改动是新增 zmm-drama 0.1.0 + zmm-path / zmm-trend 的公开元数据补齐，其余 22 个未改。

## 2026-09-22 · 新技能 zmm-trend 0.1.0：风口在哪 · 整包 v0.11.0

- 新增第 24 个技能 `zmm-trend`（詹明明·风口在哪），小老板看盘 8 → 9，整包 23 → 24。入口是**一个正在发生的大变化**（AI 来了、平台换了、行业洗牌）——「我该押什么」「这是不是风口」「我这行会被怎么改」。与 `zmm-path`（已经有目标）、`zmm-decide`（一个具体岔路口）、`zmm-portfolio`（几条线怎么分）边界互斥，是它们的上游。
- 八个动作：从一场**已经发生**的颠覆倒推机制 → 把趋势拆成 3–5 条能量的曲线（**每个数现查、带来源和日期，查不到写「待查」**）→ 把「还做不到」拆开看最后剩什么 → 找一个走完过的老产业当剧本（**「哪里不一样」一列不许空**）→ 先让自己活在未来里一段时间 → 落成「赌什么、不赌什么」→ 过位置 · 时机 · 能力组合三关 → 每条下注挂到期信号落 `/zmm-track`。
- 核心公理：判断分三层，**变量层最准、结构层次之、终局层（谁赢谁输）最不准，默认降一档说**。这条来自对一个历史预言的逐条事后核对（变量层几乎全中，终局层一半没中），完整过程见 `references/案例-手机替代电脑.md`。
- 明确拒绝「坚信它就会发生」这类不可证伪的说法：**坚持交给承诺，判断交给信号**。
- 三份 references：理论底座（每条带边界与出处强度；炒作周期、「高估短期低估长期」等单列为实务判据，不许说成研究表明）、硬变量库（AI / 内容平台 / 零售门店 / 制造各领域该盯的曲线与去哪查，**一个数都不写死**）、历史案例（八个动作走一遍 + 分层事后核对）。
- 构建闸新增一条私有泄漏模式「外部方法来源」：对外技能不写外部方法的出处（vault 规则总表 2026-09-15），讲者特有的说法同样当作指纹拦截。
- 整包 v0.11.0：唯一成员改动是新增 zmm-trend 0.1.0，其余 23 个未升版。

## 2026-09-22 · 整包 v0.10.0：23 个技能

- 整包由 22 → **23 个技能**，唯一成员改动是新增 `zmm-path 0.1.1`，其余 22 个未升版。
- 包：74 个文件（70 + `modules/zmm-path.md` 与三份 references），435,852 字节，
  SHA256 `99a56bdf50255a128239a52c6be40bfc4e07520db6db71b1d1c2322793c0658a`，
  已从 latest 固定地址重新下载逐字节核对。固定下载地址不变。
- 渠道：GitHub Release ✅ · skills.sh ✅ · ClawHub ✅（实测 `clawhub install iamzifei/zmm-path` 装得下来）·
  SkillHub 🟡 已提交 `versionId=364181` 等审核 · 豆包 / WorkBuddy 由固定地址供给 ✅。
- **新记下两件事**（详见 `docs/runs/2026-09-22-v0.10.0-release.md`）：
  ①ClawHub 对**新建**技能有一道约一分钟的短时审核，窗口内搜不到也装不了，别误判成发布失败；
  ②`skillhub publish .` 会把目录里的 `zmm-toolbox.zip` 当成待上传文件并报 400，**先发 SkillHub 再打包**。

## 2026-09-22 · zmm-path 0.1.1：首次实战暴露的三处，已回写

首跑用的是一个真实提问（业余时间做小红书育儿科普、目标一万粉、问用哪个 AI / 要规划 / 要工作流 / 要提效）。技能的关键动作都触发了——**停在关一没编规划**、逮住「一万粉 → 有收入」这个核心假设、逮住「各式各样的困难问题」这个在做工的词。暴露的三处已改：

- **补「搭车问」例外**：原文只写「一次只问一个」，而实战里自然出现了一个高价值的搭车问（「你有没有真的收到过钱」）。现在给了准入三条（一句话能答 · 会改变下一轮方向 · 不是主问题的另一种问法），且**只许搭一次**，出现第二个就是问卷。
- **补小红书式子**：`机制式库.md` 原来只有看完型（抖音/视频号）的涨粉式，漏了图文侧独有的「**点进比例**」那一格，实战中只能现推。新增点进型式子与五格因子表，并写明三条形状差异（搜索长尾 / 收藏比点赞更说明价值 / 封面是独立一关），**标注为常识与推断，非本系统实测**。原式子改标为抖音 / 视频号专用。文件末尾建回写记录表。
- **「式子可以在关一末尾预告」写进正文**：式子不需要用户回答任何问题，自己就能推。卡在基线、这一轮走不完时也要把式子摆出来、空格标问号——它让「去量」变具体，而且常常当场把用户问题的位置指出来（首跑中，提问者的四个问题全部只能动第一格，式子一摆他自己就看见了）。附硬约束：**只是预告，不许自己填估值把格子补满**。
- 整包由 22 → 23 个技能，README 各处安装说明的数量同步更新。

## 2026-09-21 · 新技能 zmm-path 0.1.0：从哪儿下手

- 新增第 23 个技能 `zmm-path`（詹明明·从哪儿下手），小老板看盘 7 → 8，整包 22 → 23。入口是**一个没有路径的愿望**（「我想赚一百万」「涨到一万粉」「营收加三成」），与 `zmm-decide`（一个具体岔路口）、`zmm-revenue` / `zmm-retro`（症状归因）边界互斥，开场先判入口再决定留不留。
- 六关：可判定目标（完成态/期限/基线）→ 拆成能分别量分别动的因子 → **一个**瓶颈 + 一个不许变坏的对照项 → 2–3 条带代价与证据强度的路径 → if-then 第一步 + 事前验尸 + 失败判据 → 落 `/zmm-track` 回看。
- 两条反闸门设计：**够清楚就放行**（下一步和完成态都明确 → 直接跳到关四/关五）；**29 问不许一次问完**（清单进 references，默认跳过第 4、6、7、13、24 问）。
- 唯一一道确认闸在关三（瓶颈要用户亲口认，「嗯」不算），理由写在正文里：它拦的不是用户去执行，是拦住技能在错的因子上给出整套路径。
- 第二交付物：一段用户可反复使用的提问模板（对象/目标/冲突/约束/反馈入口），以及「这条路上哪几段能交给 AI、哪几段只能自己去现实里取数」的三档分工。
- 新增 `references/理论底座.md`（按关映射，每条标边界与**出处强度**，并单列一节「实务判据·不是学术结论」防止顺口说强）、`references/机制式库.md`（生意/内容/门店/能力/招人的现成式子，含乘法·加法·存量三种形状之分）、`references/问题清单.md`（29 问归位 + 跳过条件 + 改良问法）。
- 对第 7 问「连续五个为什么」给出否定判断并写明理由：它只产出一条自洽的单链、全程不需要证据，与家族〈观察与解释分开〉冲突；替代问法是「这两件事有没有分开出现过」。仅机械故障排查场景保留。

## 2026-09-21 · README 重排：先说干什么，再说怎么装

- 顺序调整：原本先讲 7 种安装方式（157 行）才讲用途，改为「解决什么问题 → 怎么用 → 能力一览 → 硬规矩 → 怎么装」。
- 新增两张索引表：按用户处境索引的「卡在哪 → 拿到什么」，和按工作目标索引的 22 个技能能力表。
- 新增编排结构图 `docs/images/zmm-map.svg`，说明总入口如何按任务挑 1 主 + 最多 2 辅，以及结果回流。
- 新增豆包演示 GIF 两段：发一句带固定地址的话让豆包自己安装、装完触发技能。
- 豆包安装主路径改为「把地址发给它」，手动上传降为备选；两条路径都保留。
- `build_public.sh` 的插图复制由 `*.png` 改为全量，并加数量闸 —— 原写法会让新增的 GIF / SVG 静默漏掉。
- 技能内容未改动，整包未升版（包内容与 v0.9.1 相同）。

## 2026-09-21 · 整包 v0.9.1：hook 具体判断与固定下载地址

- 整包仍为 22 个技能，唯一成员改动是 hook 0.4.1；其余成员未升版。
- 豆包 / WorkBuddy 安装改用 Releases 的 latest 固定地址，发新版无需更换链接。
- 源码成员版本：hook 0.4.1，topic 0.5.0，title 0.2.3。整包发布号独立为 0.9.1。

## 2026-09-15 · hook 0.4.1：具体判断与完整兑现

- 将悬念生成落到具体信号、可能推断、缺失环节与实际选择；同正文比较抽象与具体入口。
- 增加同一示例的输入、判断、产物检查，避免强开头接流程目录或局部改词。
- 添加正反例演练，效果未测量。纳入整包 v0.9.1。

## 2026-09-14 · 整包 v0.9.0：问题探寻与大众悬念

- 整包收录全部 22 个技能，同步选题的问题探寻、竞争解释与共鸣规则，以及开场悬念生成和正文兑现检查。
- 源码技能版本：topic 0.5.0、hook 0.4.0、title 0.2.3；整包 Release / SkillHub / 豆包使用 0.9.0。
- 统一分发文档表述，保留方法判据与效果验证边界。

## 2026-09-14 · hook 0.4.0：大众悬念与兑现

- 完善内容试用、素材提取／补充／悬念制造，新增做法→观众目标→未解关系的生成步骤。
- 同正文比较本人材料入口与观众问题入口，逐候选记录疑问及兑现处；不再默认自曝最强。
- 真正的问句不要求先加训诫或威胁；修正主文件与规则卡的对应限制。
- 本人偏好与实测效果分开。纳入整包 v0.9.0。

## 2026-09-13 · topic 0.5.0：问题探寻与共鸣

- 完善现象定位、假设检查、竞争解释、目标与处境共鸣，增加独立参考文件并接入 A／B／C 三种模式。
- 增加六种语义问题支架；区分生成方法、本人钩子偏好和未经验证的传播效果。
- 限定「走到死路」适用范围，取消所有标题必须含情绪词的普遍要求；title 0.2.3 仅同步交接边界。
- 原有事实、定位、供需、理论核实与去重要求保留。纳入整包 v0.9.0。

## 2026-09-12 · 整包 v0.8.0：理论框架选型与装配

- 新增共享理论装配规则：区分解释内容与设计讲法，多视角比较候选，检查机制、前提、反例和边界，再映射到骨架段落。
- 接入选题、钩子、口播、概念与审核技能；沿用既有理论评分与原文核实，用户只要骨架时不提前写正文。
- 线下录音选题先识别普遍认知和问题，再判断说话人及出稿人称。
- 源码版本：zmm 0.3.0、topic/script/concept/review 0.4.0、hook 0.3.0；整包包含全部 22 个技能。

## 2026-09-06（第三批补丁二 · 0.x.2）：三条口径定案

- **收入可以说量级**：`zmm-review` / `zmm-script` 规则卡统一为「收入类不说具体数和币种，可以说六位数 / 七位数 / 翻了几倍；单价可以说具体数」，删掉临时的「审核卡优先」标注
- **最强开场有实测**：`zmm-hook` 三种起手仍不排序，但明写「祈使否定 + 后果威胁」是样本池里实测最高的开场形态，候选里至少一个用它；`zmm-title` 注明「结构不通用」说的是标题，不是开头
- **60+ 只作参考**：`zmm-review` 判「重写」改为「80+ 不到 2 句且答不出押注点」，不再用 60+ 计数

## 2026-09-06（第三批补丁 · 0.x.1）：五条「技能落后于规则」

提炼规则卡时对出来的，技能正文还停在旧口径的五处，按规则改齐：
- `zmm-script`：产出格式从「双列分镜稿」改回**整篇口播稿**（画面在稿后单列）；收尾**不写任何行动指令**，只把「我有什么」当事实带过
- `zmm-review`：第 9 问同步 —— 行动指令命中即 ❌；第 4 问明写「字数只估时长，不当判据」
- `zmm-topic/references/常识缺口法.md`：情绪版不当第一句（第一句给有解），挪到中段当替观众说的那句，标题仍从它来
- `zmm-concept`：语速结案为原速 5.3–5.8 字/秒区间，旧的「待重核」删除
- `zmm-title` + `config.example.yaml`：封面大字上限是**每行** 6 字，不是总字数

## 2026-09-06（第三批）：内置规则卡 + 三处骨架重设计

### 每个内容技能自带 `references/规则卡.md`

原来大量判据只存在于作者自己的素材库里，公开版用户拿到的技能「知道该读什么文件」却读不到那些文件。这次把 **280 张规则卡**直接内置进 14 个技能（`zmm-script` 109 张最多，`zmm-review` 34，`zmm-hook` 31，`zmm-retro` 22，`zmm` 21 …）。每张卡固定五格：**判据 / 为什么 / 怎么查 / 例子 / 强度**（硬 · 实测 · 经验）。作者环境里有对应规则文件时以那边为准，规则卡为底。各技能启动段加了一句读取顺序；家族公约 §一 加了索引行。

### `zmm-topic` 0.2.0 → **0.3.0**：模式 C 重设计为「找拉力」

原来的「话题 → 题」工具换成一个判据贯穿全文：**观众能不能同时预测出两个不同的结局**。七问：话题还是题 → 观众的默认答案 → 默认答案在什么条件下会错（三种来源）→ 对面站着谁（替对面写一句讲得通的话，且不能是目标观众）→ 压成一句会有人反驳的话 → 要改观众的哪一样 → 从哪一刀切。输出三个版本按「观众反驳的容易程度」排。旧文件删除。
- 过滤器第 5 条撞车查重的运行说明改成不依赖安装位置
- 新增 `evals/` 一份模式 C 样本

### `zmm-resonate` 0.2.0 → **0.3.0**：模式 C 重设计为「一件事还是一群人」

原来的链条换成：把「不对劲」翻成看得见的事 → 找三个彼此不像的人 → 三个人共同踩的条件（「只要…就…于是…」，标看到的还是猜的）→ 找一个满足条件却没出事的人 → 翻成观众能对自己说的一句话 → 摆链路问一句。**找不到三个不一样的人就停**，输出「这是一件事」。

### `zmm-review` 0.2.0 → **0.3.0**：从「逐关清单」改为「观众的四次决定，十一问」

审核顺序改成跟着观众走：**点不点进来**（这题有人需要吗 · 第一句钉住谁）→ **留不留下来**（逐句密度 · 哪里在空转 · 听不听得懂 · 骨架与断点）→ **记不记得你**（说法是不是老话 · 站在谁那边 · 凭什么是你）→ **做不做点什么**（押注点 · 出口与红线）。判据一条没少，只换了组织方式和措辞；稿型分类改为「用力方向」。

### `zmm-script` 0.2.0 → **0.3.0**：骨架改名「亮路径三幕」

九步骨架按「撞上 / 想通 / 做完」三幕重组，第二幕（我是怎么一步步想的）标为全稿唯一别人抄不走的部分。`zmm-hook` / `zmm-product` / `认知框架.md` 同步。

### `zmm-hook` `zmm-mvp` `zmm-concept` `zmm-title` `zmm-flow` `zmm-cut` `zmm-retro` `zmm-benchmark` `zmm-product` `zmm-skillify` `zmm`：minor 升版

- 开头候选体系改叫**三种起手**（先让情绪失衡 / 先点名是谁 / 先反着说）+ **四种留白**
- 各自接入规则卡；若干措辞与例句重写

### 仓库

- `scripts/check_paths.py` 支持公约表里的通配行
- `scripts/overlap_scan.py`：两棵目录树之间的整句重合扫描（不发布）

---

## 2026-09-06（第二批）

### `zmm-flow` 0.1.0 → **0.2.0**：从「段落打分」改成「替观众听一遍」

原来按段落切、每段过三个维度打分。现在**按观众在等的东西变了切拍**，每拍记四样：给了什么 · 观众手里有 · 观众在等 · 状态。断点只有三种，全部从「手里有 / 在等」推出来：**等的没来 · 手里有了还在给 · 正在给的听不清**。删拍前必过承重检查（遮掉这拍，下一拍手里缺不缺东西）。
- 新增 `evals/`：一份埋了 4 处缺陷 + 2 个陷阱（白板口令、承重案例）的样本。旧版与新版各由没看过答案的新 context 跑一遍：**两版 4/4 命中、0 误报，新版另抓到两处「等的没来」**。结果与答案都在目录里

### `zmm-resonate` 0.1.0 → **0.2.0**：从「理论打分」改成「观众带走了什么」

原来五个理论逐维给「有效 / 弱 / 无效」，工具型干货稿也要硬答「谁会第一个转它」。现在两张表：**三种收获**（原来是这样 / 我就说吧 / 他替我说了）和**四种冲动**（收藏 / 转发 / 评论 / 关注，互相独立不推导）。每条判断分三层写：**原文 · 推断（置信度）· 待验证**，情绪只能写成「可能让人」。理论退到 `内容理论底座.md` §七 的脚注编号（M1–M6）。
- 新增 `evals/` 一份样本与答案

### `zmm-review` 0.1.0 → **0.2.0**：机器信号层 + 两版改法

- **Phase 3b 机器会先看到什么**：导流痕迹 / 广告形状 / 名单词 / 画面层，单独一节，**只回答「可能被机器拦吗」，不许替它编内容违规的理由**；只查了文字稿就必须写「封面、主页、评论区、画面本次未纳入」。机器命中不参与三档判定
- **红线改法给两版**：稳妥版 + 保留力度版；缺边界会改变理解时可加「画面补边界」两三行小字，口播不动
- `config.example.yaml` 新增 `redlines.machine_signals`（导流正则 / 广告五件 / 名单词）
- 新增 `evals/` 一份含导流 + 卖真 + 违禁词的样本

### `zmm-decide` 0.1.0 → **0.2.0**：拆到本质补三道加问

B1–B3 拆的是措辞，新增 **B3b** 拆地基：把「一起出现」当「导致」了吗 · 前提是别人说的没核过吗 · 现有信息够不够任何人判断。命中就换出口：**「先去核那个数」「先去做一次拿数」是合格结论**，不进推演。

### `zmm-concept` 0.1.0 → **0.2.0**：理论卡加「核到哪一级」

第 3 样的每条理论配一张卡：解决什么 / 机制 / 出处 / **核到哪一级（读过原文 · 权威二手 · 没核到）** / 原话还是意译。两次核不到就停，不进成稿点名；转引写「转引自」。

### `zmm-skillify` 0.1.0 → **0.2.0**：生成之后那半段

新增 **Step 5**：四级验证梯子（静态过 → 新 context 跑通一次 → 样本组含近邻反例 → 改版不退步），爬到哪级只许说哪级的话；`evals/` 样本约定（正例 · 边界 · 近邻反例，答案只给评分者，结果按 `version` 归档）；失败先归类（指令 / 触发 / 输入 / 判据 / 波动）再改，同一失败连改三版还在加规则就回 Step 1。

### `zmm-topic` `zmm-script` `zmm-retro` `zmm-revenue` 0.1.0 → **0.2.0**

- topic 过滤器补第 5 条 **撞车查重（强制）**：先跑重建脚本，再拿「核心结论 + 主类比」对两张表比；script 的加载清单同步
- retro / revenue 的归因、decide 的判据统一按家族公约新增的**〈归因四格〉**写：怎么导致的 / 成立能看到什么 / 它排除了谁 / 信了下一步变什么

### 家族公约

- §一 加载表有了机器核对：`scripts/check_paths.py`
- §四 新增〈归因四格〉
- §六 记忆改**追加式**：修正在旧记录下方加 `## 修正 日期`，原段不动，不删文件；每条标 `依据: 本人原话 | 平台数据 | AI 推断`，AI 推断只能是 `待验证`

### 仓库

- 新增 `scripts/check_paths.py`：把公约 §一 的每条路径和 `config.paths.*` 逐个 stat，缺一条非零退出（对真实 vault 跑：49 项全在；故意改坏一条，报出来）
- 新增 `scripts/rebuild_published_index.py`：从已发布文件夹重建 `*.自动.md`，并报手工表漏登记的、指不到的、frontmatter 缺字段的（负向测试：塞一个没登记的文件夹，退出码 1）
- `build_bundle.py` 接受第三个参数「发布号」，打包版 version 不再读 hub 的号
- `config.example.yaml` 补齐公约引用的五个 `paths` 键（voice / format / feedback_rules / published_index / retro）
- 公开仓 `iamzifei/zmm` 历史压成单提交，Release 只保留 v0.5.1

---

## 2026-09-06

### 措辞与结构梳理（版本号不动，行为不变）

对四个技能里几段「规则写得像别人家的规则」的地方做了改写，判据本身没变：

- `zmm-flow`：划走点三类改叫 **台阶缺失 / 内容空转 / 念不出口**；删段前的必过一问改成「遮掉这段，观众补不补得齐」；
  白板 vs 纯口播的判据切换说清了**为什么**相反（板书有笔可指，口播没有）；报告模板改为「稿子骨架 / 划走点 / 一句话结论」
- `zmm-hook`：开头三要素改成观众心里的三个问题（讲什么 / 为什么看 / 凭什么信）；
  钩子强度从六类星级表改为**按「观众要不要先信你」分三档**（自证 / 对比 / 借力），卖真警戒随之改写；候选三来源重述
- `zmm-title`：「5 铁律」改为「出手前的 5 道关」，每一关配一个自测问句；收敛到 3 个时说清各自换来的是什么
- `zmm` 总入口：组合判定从五个条件改为**五道否决题**；辅技能按前 / 中 / 后三个位置定角色；提示词模板措辞重写
- `zmm/references/内容理论底座.md`：理论出处改用中文名 + 年份

### 仓库

- `scripts/leak_scan.py` 拆成两层：**框架**随包发布，**私有模式表**（`leak_patterns.private.py`）永不发布 ——
  公开版扫描器不再自带一份「它要藏的名字」清单；私有表缺席时扫描报告会明说只扫了金额 / 密钥 / 路径
- 扫描范围补 `.yml`；私有模式表补了几个此前漏网的裸词形式

---

## 2026-09-05

### `zmm-cut` 0.1.0 → **0.2.0**

**这次改动来自一次真实的成片剪辑**（两条素材合成一条 2 分 30 秒的成片），
每一条都是当时踩到或验证过的，不是设想出来的。

#### 新增能力：按文案规则做内容层重组

原来只有**机械层**（去固定口癖、批量压停顿）。新增**内容层** —— 哪些留、哪些删、哪些换位置。

- 新增 `references/内容层重组.md`：通用流程 + 五条判据
  （识别废镜头 · 逐字重复扫描 · 推理链不许为悬念重排 · 开场要同时有钩子和共鸣 · 删之前问「删掉之后还讲得清楚吗」）
- 🔴 **边界写进正文**：**只删和重排，不加词**。原来只写「本技能不改文案」，
  会被读成「什么都不许动」，和这个能力打架

#### 新增：环境降级路径

- 检测不到剪辑软件时，**引导安装**（路径照抄官方文档，不自己编）
- 🟢 **给出本机转写旁路**：内容层的活全部不需要那个软件 ——
  本机 whisper 出文字稿 → 全部判断照做 → 软件装好后只用来落时间轴
- ⚠️ **明确不写**「在软件里选本机模型省钱」这类说法 —— 查了官方文档**没有这个功能**，
  查不到依据的东西不进技能

#### 新增：`references/ChatCut实操.md`

三个实测踩过的工具坑（各附取证方法）、验收纪律、B-roll 实操、导出验收。

#### 修正

- 🔴 **删掉一个不存在的前置技能引用** —— 原 §〇 要求先加载某个插件版前置技能，
  实测在 MCP 直连环境下**那个技能不存在**。改成先探环境再决定怎么走
- 工作流 **五步 → 七步**：补 `⓪ 素材清点与合并`（原来默认只有一条素材，
  实际常常是原片 + 补拍）和 `⑥ 导出后验文件本身`
- 新增 `§二b 验收纪律`：**元数据会说谎**，行数/文字/画面只能以全尺寸渲染帧为准
  （实测三次被工具回执和缩略图骗到）
- `§一 账号默认参数` 标注为**某一个账号的实测值，不是通用推荐**；
  有 `config` 就从 `config` 读。**换账号只改那张表，正文流程不变**

### 仓库

- 新增本文件 `CHANGELOG.md`，并加进 `build_public.sh` 的发布白名单
- `README.md` 的 `zmm-cut` 条目改写，反映新能力
